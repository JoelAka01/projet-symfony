{% extends 'base.html.twig' %}

{% block title %}SEO GEO AI Platform — Boost your search & AI visibility{% endblock %}

{% block main_class %}lp-page{% endblock %}

{% block body %}

{# ── HERO ─────────────────────────────────────────────────────────────────── #}
<section class="lp-hero">
    <div class="lp-hero__inner">
        <h1 class="lp-hero__title">
            Rank on search engines.<br>
            <span class="lp-accent">Get cited by AI.</span>
        </h1>
        <p class="lp-hero__sub">
            Audit your website's SEO and monitor how ChatGPT, Gemini, and other AI engines recommend your brand.
        </p>
        {% if app.user %}
            <div class="hero-actions">
                <a class="button primary lp-btn-lg" href="{{ path('app_project_index') }}">
                    Open projects →
                </a>
            </div>
        {% else %}
            <div class="hero-actions">
                <a class="button primary lp-btn-lg" href="{{ path('app_register') }}">
                    Get started free
                </a>
                <a class="button secondary lp-btn-lg" href="{{ path('app_login') }}">
                    Sign in
                </a>
            </div>
            <p class="lp-hero__note">No credit card required.</p>
        {% endif %}
    </div>
</section>

{# ── FEATURES ─────────────────────────────────────────────────────────────── #}
<section class="lp-features">
    <p class="lp-section-label">Features</p>
    <h2 class="lp-section-title">Everything you need, in one place</h2>
    <div class="lp-feature-grid">

        <div class="lp-feature-card">
            <div class="lp-feature-icon">
                <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                    <circle cx="11" cy="11" r="8"/><path d="M21 21l-4.35-4.35"/>
                </svg>
            </div>
            <h3>SEO Audit</h3>
            <p>Scan your website, find technical issues, and get direct recommendations.</p>
        </div>

        <div class="lp-feature-card">
            <div class="lp-feature-icon">
                <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                    <path d="M12 2L2 7l10 5 10-5-10-5z"/><path d="M2 17l10 5 10-5"/><path d="M2 12l10 5 10-5"/>
                </svg>
            </div>
            <h3>AI Search (GEO)</h3>
            <p>Monitor if and how major AI models like ChatGPT and Gemini recommend your brand.</p>
        </div>

        <div class="lp-feature-card">
            <div class="lp-feature-icon">
                <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                    <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/>
                </svg>
            </div>
            <h3>AI Content</h3>
            <p>Generate optimized articles with FAQs and structured data ready to publish.</p>
        </div>

    </div>
</section>

{# ── CTA BOTTOM ───────────────────────────────────────────────────────────── #}
{% if not app.user %}
<section class="lp-cta">
    <h2>Ready to optimize your website?</h2>
    <p>Sign up now to scan your pages and check your AI presence.</p>
    <div class="hero-actions lp-cta__actions">
        <a class="button primary lp-btn-lg" href="{{ path('app_register') }}">Get started free</a>
        <a class="button secondary lp-btn-lg" href="{{ path('app_login') }}">Sign in</a>
    </div>
</section>
{% endif %}

{% endblock %}
